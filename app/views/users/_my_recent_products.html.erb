    <ul>
      <% if cookies[:last_visited] %>
      <%  Product.where('id in (' + cookies[:last_visited] + ')').each do |p| %>
      <li><%= link_to p.name, p %> <span class="price"><%= p.price %></span></li>
      <% end %>
      <% end %>
    </ul>
